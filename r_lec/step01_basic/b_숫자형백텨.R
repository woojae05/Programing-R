# numeric vector

v1 <- c(-1,0,1)
v1

age<-c(21,15,23,35,50)
class(age)

# str()는 자료형, 데이터의 길이, 데이터값을 전체적으로 확인 가능
str(v1) #변수의 속성확인

typeof(v1) #자료형 확인
length(v1) #벡터의 길이 확인

#------------------------------------------
#실수형 백터
dv<-c(10.1,20.2,30.3,40.4)
str(dv)
# 행렬